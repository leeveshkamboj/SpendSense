<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <style name="WidgetProgressBar" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/widget_pill_progress_bar</item>
        <item name="android:minHeight">44dp</item>
        <item name="android:maxHeight">44dp</item>
        <item name="android:indeterminateOnly">false</item>
    </style>
    <style name="WidgetPillProgressBar" parent="@style/WidgetProgressBar" />
    <style name="WidgetPillProgressBarGreen" parent="@style/WidgetProgressBar">
        <item name="android:progressDrawable">@drawable/widget_pill_progress_green</item>
    </style>
    <style name="WidgetPillProgressBarYellow" parent="@style/WidgetProgressBar">
        <item name="android:progressDrawable">@drawable/widget_pill_progress_yellow</item>
    </style>
    <style name="WidgetPillProgressBarRed" parent="@style/WidgetProgressBar">
        <item name="android:progressDrawable">@drawable/widget_pill_progress_red</item>
    </style>
</resources>
